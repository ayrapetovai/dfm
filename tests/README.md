To execute tests with debug output for failed tests run:

```shell
./launcher.sh
```
To execute tests without debug output run:

```shell
./launcher.sh -q
```

For a specific test run:

```shell
./launcher.sh test_file_name.sh
```

For a specific test with no debug output run:

```shell
./launcher.sh -q test_file_name.sh
```

